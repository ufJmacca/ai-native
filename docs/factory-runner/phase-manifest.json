{
  "manifest_version": 1,
  "plan": "docs/factory/AI_NATIVE_FACTORY_RUNNER_IMPLEMENTATION_PLAN.md",
  "protocol": "factory-runner-protocol/v1",
  "repository": "ufJmacca/ai-native",
  "merge_policy": {
    "mode": "trusted_cli",
    "merge_method": "merge",
    "required_checks": [
      "test",
      "lint",
      "factory-runner-ci"
    ],
    "reviews_required": 0,
    "exact_head_required": true,
    "admin_bypass_forbidden": true,
    "branch_protection": "enforce_when_available"
  },
  "phases": [
    {
      "id": "AN-00",
      "title": "Baseline characterisation and boundary lock",
      "depends_on": [],
      "branch": "factory-runner/an-00-boundary",
      "completion_gate": "Required automated checks and machine-verifiable phase evidence pass for the exact PR head; the trusted publisher performs the trusted CLI merge through the policy-gated SHA-bound merge endpoint; the resulting merge commit is verified as the exact default-branch HEAD with the boundary ADR"
    },
    {
      "id": "AN-01",
      "title": "Factory runner protocol v1 contracts",
      "depends_on": [
        "AN-00"
      ],
      "branch": "factory-runner/an-01-contracts",
      "completion_gate": "Required automated checks and machine-verifiable phase evidence pass for the exact PR head; the trusted publisher performs the trusted CLI merge through the policy-gated SHA-bound merge endpoint; the resulting merge commit is verified as the exact default-branch HEAD with stable v1 schemas"
    },
    {
      "id": "AN-02",
      "title": "Non-interactive factory runner",
      "depends_on": [
        "AN-01"
      ],
      "branch": "factory-runner/an-02-runner",
      "completion_gate": "Required automated checks and machine-verifiable phase evidence pass for the exact PR head; the trusted publisher performs the trusted CLI merge through the policy-gated SHA-bound merge endpoint; the resulting merge commit is verified as the exact default-branch HEAD with unattended author and verify operations"
    },
    {
      "id": "AN-03",
      "title": "Event, checkpoint, evidence and change-set outputs",
      "depends_on": [
        "AN-02"
      ],
      "branch": "factory-runner/an-03-outputs",
      "completion_gate": "Required automated checks and machine-verifiable phase evidence pass for the exact PR head; the trusted publisher performs the trusted CLI merge through the policy-gated SHA-bound merge endpoint; the resulting merge commit is verified as the exact default-branch HEAD with validated content-addressed outputs"
    },
    {
      "id": "AN-04",
      "title": "Release packaging and compatibility certification",
      "depends_on": [
        "AN-03"
      ],
      "branch": "factory-runner/an-04-release",
      "completion_gate": "Required automated checks and machine-verifiable phase evidence pass for the exact PR head; the trusted publisher performs the trusted CLI merge through the policy-gated SHA-bound merge endpoint; the resulting merge commit is verified as the exact default-branch HEAD before the post-merge workflow produces an independently verified immutable receipt-backed release"
    }
  ]
}
