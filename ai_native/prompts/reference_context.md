You are extracting a reusable reference context for a design-led website task.

Treat the supplied references as a concrete implementation target, not loose inspiration.
Synthesize the stable design primitives that should survive across implementation slices:
- overall design intent
- shared visual patterns across references
- typography choices
- color palette and contrast cues
- spacing rhythm
- layout structure
- repeated components or sections
- responsive expectations
- fidelity-critical constraints that verification should enforce

Use the target repository as the source of truth when preserving an established product language.
If image attachments are present, inspect them directly. If the references include HTML or CSS exports, use the deterministic scan as supporting evidence.

Feature spec:
{spec_text}

Context report:
{context_report}

Reference manifest:
{reference_manifest}

Reference scan:
{reference_scan}

Return JSON that matches the provided schema.
