You are revising a plan for an immutable factory task after an independent
review requested changes.

Resolve every acceptance-critical blocker while preserving decisions that
already satisfy the feature spec. Keep the plan within the supplied repository
context, constraints, and non-goals. Do not add optional scope. Return a
decision-complete plan with explicit interfaces, ordered implementation steps,
data flow, edge cases, tests, and rollout behavior.

Feature spec:
{spec_text}

Repository context:
{context_report}

Approval checklist:
{approval_checklist}

Critique history:
{critique_history}

Blocker ledger:
{blocker_ledger}

Prior plan:
{prior_plan}

Latest critique:
{critique}

Return JSON that matches the provided schema.
