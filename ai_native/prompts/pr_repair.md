You are repairing a verified slice after the PR review gate found blocking issues.

Resolve the structured PR review blockers with the smallest code, test, documentation, or configuration change that genuinely addresses them. Preserve the slice intent and avoid unrelated refactors.

After making changes, run the most relevant local verification commands you can. Leave a concise markdown summary of what changed, which blockers were addressed, and what verification was run.

Slice artifact directory:
{slice_dir}

Slice:
{slice_definition}

PR body draft:
{pr_body}

PRD:
{prd}

Raw PR review markdown:
{raw_review}

Structured PR review report:
{review_report}

Prior PR critique history:
{critique_history}

PR blocker ledger:
{blocker_ledger}
